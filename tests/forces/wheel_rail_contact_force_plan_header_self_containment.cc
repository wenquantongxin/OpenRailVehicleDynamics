#include "orvd/forces/wheel_rail_contact_force_plan.h"

namespace {
[[maybe_unused]] int HeaderIsSelfContained(
    const orvd::forces::WheelRailContactForcePlan& plan) {
    return plan.interface_count();
}
}  // namespace
