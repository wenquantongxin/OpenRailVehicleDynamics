# /goal-loop — Goal 循环推进规程

按路书逐 Goal 推进本仓库:每轮以 Codex 裁决开工、以 Codex 复核收尾,无阻塞则自动进入
下一 Goal。本文件既是规程文档,也是可执行提示词:收到 `/goal-loop` 即从「第 0 步」开始
执行路书当前 Goal 的一轮;循环由 Claude 驱动,Codex 是裁决与复核方,用户随时可中断。

## 固定量

```
路书        docs/planning/DRAKE_MULTIBODY_RUNTIME_DECOUPLING_ROADMAP.md(唯一进度权威,「当前 Goal」条目)
Codex 线程  019f8f8b-6fb9-7291-9551-04079771d326(零 Drake 重构方案)
Codex 模型  gpt-5.6-sol,model_reasoning_effort=ultra(单次分析普遍 10–20 分钟)
交接目录    ~/.codex/handoff/(仓库外、跨会话稳定;scratchpad 随会话销毁,不可用)
交接文件    每轮四件:GXX_ruling.stage.txt / GXX_ruling.out.log / GXX_review.stage.txt / GXX_review.out.log
```

## 不变量(任一被破坏即阻塞)

- 只提交 `main`,永不 push,不建分支与 PR。
- wheel-rail-lab、Drake 源码克隆、`/opt/drake` 只读;仓外探针不落入任何仓库。
- 仓库内不写机器路径;Drake 位置只经 `CMAKE_PREFIX_PATH` 等标准 CMake 参数传入。
- Codex 汇报的一切承重数字(通过数、退出码、计数)在采信前第一手复跑;
  呈报用户时「其声称 / 我复跑」两列并排。
- 用户工程铁律优先于 Codex 裁决:语义命名(禁版本号与 classic/new 式区分)、零兼容层、
  对可信内部输入宽松解析、fail fast、相对误差 ≤1e-3 的验收口径、无金标/哈希/快照。
  冲突即执行铁律,并在下一轮载荷中说明。
- 向 Codex 发送前工作区必须干净:该线程以 `approval_policy=never` + danger-full-access
  运行,会直接改码、改提交(包括 amend 重写 HEAD)。

## 一轮六步(G := 路书当前 Goal)

0. **前置**:`git status --porcelain` 为空;路书当前 Goal 与本轮一致;上一轮 Codex 的
   全部改动(含被 amend 的提交)都已审读消化。
1. **侦查**:通读 G 的产物/完成门/明确不做;通读将触及的文件与相邻代码;必要时做只读
   探针。产出四件:已核实事实、困惑点与我的倾向、实施草案、完成门清单。
2. **裁决请求**:按模板 A 写 `GXX_ruling.stage.txt`,后台发送,等完成,取最后一个
   codex 块。模板 A 明令 Codex 本轮不改码。
3. **实施**:按裁决(与铁律)落码;判别力自证至少一次(故意破坏 → 必须失败 → 还原 →
   必须通过);全部完成门跑通;更新路书勾选与当前 Goal;提交 `main`。
4. **复核请求**:按模板 B 写 `GXX_review.stage.txt`,后台发送,等完成,取结论。
   模板 B 允许 Codex 自行最小修复。
5. **收尾判定**:复跑其声称的全部门与负控并两列对照;审读其每一处改动(命名违例、
   防御性堆积、兼容层 = 立即小规模重构);向用户简报;无阻塞 → G+1,回到第 0 步。

## 发送与等待(会话机制)

写载荷一律落文件,不走命令行参数:用 Write 工具直写,或 shell 内
`cat > 文件 <<'PAYLOAD'`(PAYLOAD 的引号必须有——关闭一切变量与反引号展开)。

发送命令经 Bash 的 `run_in_background` 执行,**不要**写尾部 `&`:harness 跟踪该进程,
退出时自动唤醒会话;写了 `&` 则命令立即返回,唤醒信号丢失,只能盲等。

```bash
cd /home/yaoyao/Documents/myProjects/OpenRailVehicleDynamics && codex exec resume 019f8f8b-6fb9-7291-9551-04079771d326 -m gpt-5.6-sol -c model_reasoning_effort=ultra - < ~/.codex/handoff/GXX_ruling.stage.txt > ~/.codex/handoff/GXX_ruling.out.log 2>&1
```

- UUID 必须写全:短前缀会被当线程名解析而失败,且 UUIDv7 带时间前缀,同秒会话会撞前缀。
- `-m` / `-c` 对既有线程是冗余保险,保留不删。
- 超过约 40 分钟未被唤醒才做活性检查,任一在涨即活:
  `ps -eo pid,etime,cmd | grep '[e]xec resume 019f8f8b'`;`wc -c` 该轮 out.log。
- 取结论:`grep -n '^codex$' <out.log>` 定位,读最后一块。`exec` 独占行是命令回显,
  不是结论。VS Code 侧 app-server 不自动刷新,也不妨碍 resume,两边互不干扰。

## 模板 A — 裁决请求(Codex 只裁决,不改码)

```
# GXX 开工侦查报告(请求裁决)

## 上轮收尾确认
<其上轮修复经我复跑核实的结论,两三行>

## 路书目标(原文)
<G 的产物/完成门/明确不做,原文引用>

## 侦查所得(已第一手核实)
<事实清单,带出处>

## 困惑点与我的倾向
1. <问题>(倾向:<方案与理由>)

## 实施草案与完成门清单
<草案要点;完成门逐条>

---
## 处置要求
本轮只做裁决:对每个困惑点给出明确裁决与理由;发现本报告与代码事实不符请直接指出。
本轮不要修改代码、不要提交;实施由 Claude 完成后另发复核请求。
如认为目标需要架构级调整,停在说明层面,单独陈述。
```

## 模板 B — 复核请求(Codex 可自行最小修复)

```
# GXX 实施汇报(请求复核与扫尾)

## 阶段进展
<提交哈希与主题;完成门逐条结果>

## 代码改动面
<新增/修改文件清单>

## 关联修改及原因
<README、预设、路书等连带变化,为什么>

## 判别力自证
<破坏了什么 → 失败信息 → 还原 → 通过>

## 缺陷与修复 / 其他说明
<实施中暴露的问题、根因、修法;遗留与假设>

---
## 处置要求
上述为汇报文字,请按代码事实复核,不要采信表述。
发现问题请自行最小修复并说明:缺陷现象、根因、改动位置、修复后如何验证、验证结果。
架构级变更先停下说明,不要径自实施。
修复后重跑相关完成门并给出通过/失败结论。
```

## 阻塞条件(停循环,报告用户,等待指示)

需要 sudo、断网、磁盘或关键库缺失;codex 线程无法 resume;我的复跑与其声称不符且无法
归因;其提出架构级变更(超出路书与已接受 ADR);用户铁律与裁决冲突且属未定事项;
同一 Goal 两轮复核仍不收敛。

## 批次

默认一轮一个 Goal。已裁定的合批照准(G09+G10);新的合批须在上一轮模板 A 中提案并
获裁决,不得现场合并。
