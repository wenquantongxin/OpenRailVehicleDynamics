# Which Drake source files the ORVD rigid multibody runtime admits, and why.
#
# This file is the sole authority for the vendor boundary and for the exact
# upstream revision. Nothing else in the repository restates those values: the
# same fact written twice drifts, and the drifted copy gets used as truth.
#
# Read by tools/drake_source_boundary/calculate_required_drake_source_closure.py,
# which walks include and implementation edges from every `vendor` entry. An edge
# to a file no line here classifies is an error, and so is an edge to a
# `forbidden` one. All paths are relative to the Drake source root.
#
# Dispositions:
#   vendor         copied into ORVD and compiled.
#   first_party    not copied; ORVD implements this role itself.
#   reference_only not copied and not compiled; read to guide an ORVD pass.
#   discard        not needed today. Not an architectural violation; a later
#                  goal may admit it after a fresh decision.
#   forbidden      violates the runtime boundary. The closure reaching it is a
#                  defect, not a decision to revisit.
#
# `discard` and `forbidden` are deliberately distinct. Collapsing them would make
# "we do not need this" and "this must never be here" produce the same
# diagnostic, and the second one is the only one worth waking someone up for.
#
# Only negative category rules take a prefix. There is deliberately no
# `vendor_prefix`: admitting a directory wholesale would silently admit whatever
# upstream adds to it later, which is exactly the case the unclassified-edge
# failure exists to catch.

source_commit 231c260201ee2f7d101a8d9ccede78626f7ca13a
source_tag v1.54.0
license_spdx BSD-3-Clause
license_file LICENSE.TXT

# Drake carries no per-file copyright headers (0 of the 137 candidate files
# contain one), so attribution rides on the repository-level license plus the
# per-file upstream paths recorded below. Inventing per-file headers upstream
# never wrote would misstate the provenance, not preserve it.

# --- Category exclusions ------------------------------------------------------
# These are architecture, not preference. Each names a subsystem whose types
# would drag a dependency the ORVD runtime is defined not to have.

forbidden_prefix geometry/  # scene graph, proximity queries and mesh types; the runtime is geometry-free by construction
forbidden_prefix multibody/fem/  # finite element models for deformable bodies; ORVD is rigid-body only
forbidden_prefix multibody/plant/  # MultibodyPlant is the System-level assembly ORVD replaces with its own model-neutral layer
forbidden_prefix multibody/contact_solvers/  # contact solvers and their SAP/TAMSI machinery; no contact in the admitted runtime
forbidden_prefix multibody/meshcat/  # visualization
forbidden_prefix solvers/  # mathematical program formulations and solver bindings
forbidden_prefix geometry/proximity/  # mesh proximity internals reached only through geometry types

# --- Split implementations ----------------------------------------------------
# A header whose implementation does not share its stem. The closure cannot infer
# these, so an omission here becomes a link failure much later; declaring them
# makes the analyzer walk into them now.

implementation multibody/tree/body_node_impl.h multibody/tree/body_node_impl_mass_matrix.cc
implementation multibody/topology/spanning_forest.h multibody/topology/spanning_forest_debug.cc

# link_joint_graph_debug.cc is deliberately not declared here. It implements only
# GenerateGraphvizString and MakeGraphvizFiles, which link_joint_graph.h declares
# but nothing on the rigid solve path calls; declaring the relation would make the
# closure demand a file we already decided against. If that call ever appears, it
# surfaces as an undefined symbol at link time, which is what G11 and G17 are for.

# --- Per-file dispositions ----------------------------------------------------

# topology
vendor multibody/topology/forest.h  # Umbrella header that sets DRAKE_MULTIBODY_TOPOLOGY_FOREST_INCLUDED and pulls spanning_forest.h plus the nested Mobod/Tree/LoopConstraint/inlines fragments in the one include order that actually compiles.
vendor multibody/topology/graph.h  # Umbrella header that sets DRAKE_MULTIBODY_TOPOLOGY_GRAPH_INCLUDED and pulls link_joint_graph.h plus the nested Link/LoopConstraint/Joint/inlines fragments in dependency order; every user of LinkJointGraph includes this file, not the fragments.
vendor multibody/topology/link_joint_graph.cc  # Implements AddLink/AddJoint/RemoveJoint/ChangeJointType, AddShadowLink for loop breaking, AddToWeldedLinksAssembly accumulation, RenumberMobodIndexes after depth-first reordering, and the graph queries FindPathFromWorld/FindFirstCommonAncestor/FindSubtreeLinks/CalcLinksWeldedTo.
vendor multibody/topology/link_joint_graph.h  # Declares LinkJointGraph: AddLink/AddJoint/RegisterJointType, the JointTraits record (name, nq, nv, has_quaternion) that drives coordinate counting, WeldedLinksAssembly grouping, ephemeral shadow-link/joint bookkeeping, and ownership of the SpanningForest that MultibodyTree's mobilizer numbering is derived from.
discard multibody/topology/link_joint_graph_debug.cc  # Contains only GenerateGraphvizString() and MakeGraphvizFiles(), which write .dot files and shell out via system() to the Graphviz 'dot' binary to render pngs; nothing here computes graph or forest structure.
vendor multibody/topology/link_joint_graph_defs.h  # Defines JointTraitsIndex/WeldedLinksAssemblyIndex/LoopConstraintIndex and the LinkFlags/JointFlags/ForestBuildingOptions bitmask enums (kStatic, kMassless, kMustBeBaseBody, kUseFixedBase, kUseRpyFloatingJoints, kOptimizeWeldedLinksAssemblies) that steer how BuildForest chooses base bodies and composites.
vendor multibody/topology/link_joint_graph_inlines.h  # Inline accessors that resolve LinkIndex/JointIndex through the index-to-ordinal maps to Link/Joint references, plus set_primary_mobod_for_link and set_mobod_for_joint used on the hot path of forest building.
vendor multibody/topology/link_joint_graph_joint.h  # Defines LinkJointGraph::Joint holding parent/child LinkIndex (the user's sign convention), the JointTraitsIndex used to test for welds, connects() adjacency queries, and the how_modeled_ variant recording which Mobod (or none, for an unmodeled weld) realizes this joint.
vendor multibody/topology/link_joint_graph_link.h  # Defines LinkJointGraph::Link holding the joints_as_parent/joints_as_child adjacency lists that the forest traversal walks, attached loop constraints, the mobod_ back-reference set during modeling, welded-links-assembly membership, and the massless/static/shadow flags.
vendor multibody/topology/link_joint_graph_loop_constraint.h  # Defines LinkJointGraph::LoopConstraint, the graph-level weld record pairing a primary link with the shadow link created when a cycle was cut, fixing the parent/child ordering that sets the constraint multipliers' sign convention.
vendor multibody/topology/spanning_forest.cc  # Implements BuildForest: base-body choice policy and ConnectLinksToWorld, level-by-level ExtendTrees, HandleLoopClosure via AddShadowMobod, GrowAssemblyMobod for welded composites, CreateDepthFirstReordering with FixupForestToUseNewNumbering, and AssignCoordinates that lays out q and v.
vendor multibody/topology/spanning_forest.h  # Declares SpanningForest: the depth-first Mobod ordering, per-tree partitioning of Mobods, the q/v coordinate assignment and its inverse maps (q_to_mobod, v_to_mobod, link_to_tree_index), welded-mobods groupings, and path/common-ancestor queries — this is the topology that MultibodyTree's inboard-outboard recursions iterate over.
vendor multibody/topology/spanning_forest_debug.cc  # Sole definition of SpanningForest::SanityCheckForest(), which vendored spanning_forest.cc:176 calls via DRAKE_ASSERT_VOID at the end of BuildForest() — armed in every non-NDEBUG build, so omitting it is a link error unless vendored source is patched; it re-verifies assembly levels, mobod inboard/outboard consistency and the q/v coordinate assignment, and depends only on drake_assert.h and topology/forest.h, both already vendored.
vendor multibody/topology/spanning_forest_inlines.h  # Inline lookups defined after Mobod/Tree are complete: mobods(index), mobod_to_active_link_ordinal, mobod_to_link_ordinals, and the q_to_tree_index/v_to_tree_index/link_to_tree_index maps that per-tree kinematics and mass-matrix loops index with.
vendor multibody/topology/spanning_forest_loop_constraint.h  # Defines SpanningForest::LoopConstraint pairing the primary and shadow MobodIndex for a cut link, with FixupAfterReordering to remap both indices after depth-first renumbering.
vendor multibody/topology/spanning_forest_mobod.cc  # Out-of-line Mobod constructors (the World special case with zero coordinates, and the general case taking link/joint ordinals plus level and reversed flag) and FixupAfterReordering/RenumberMobodIndexVector that remap inboard and outboard indices after the depth-first renumber.
vendor multibody/topology/spanning_forest_mobod.h  # Defines SpanningForest::Mobod, the forest node carrying inboard_mobod/outboard_mobods, level, the q_start/nq and v_start/nv coordinate slices, the inboard path counts nq_inboard/nv_inboard (World to here, inclusive), the outboard subtree counts nq_outboard/nv_outboard and num_subtree_mobods with the contiguous subtree_velocities()/outboard_velocities() ranges that subtree passes slice with, the is_reversed flag telling the mobilizer its inboard/outboard order is flipped from parent/child, and the follower link ordinals of a composite.
vendor multibody/topology/spanning_forest_tree.h  # Defines SpanningForest::Tree, giving each tree its contiguous base_mobod..last_mobod range, height, begin/end iteration over that range, and the per-tree q_start/nq and v_start/nv slices used for tree-local state blocks.

# tree
vendor multibody/tree/acceleration_kinematics_cache.cc  # Contains only the DRAKE_DEFINE_CLASS_TEMPLATE_INSTANTIATIONS_ON_DEFAULT_SCALARS explicit instantiation of AccelerationKinematicsCache, which ORVD narrows to double alone.
vendor multibody/tree/acceleration_kinematics_cache.h  # Defines AccelerationKinematicsCache holding the per-MobodIndex A_WB spatial-acceleration pool plus the model-wide vdot vector that the base-to-tip acceleration pass and ABA forward dynamics write into.
vendor multibody/tree/articulated_body_force_cache.cc  # Contains only the explicit template instantiation of ArticulatedBodyForceCache on the default scalars, which ORVD narrows to double alone.
vendor multibody/tree/articulated_body_force_cache.h  # Stores the ABA bias terms Zplus_PB_W (residual spatial force projected across the inboard mobilizer) and e_B (innovations generalized force) indexed by MobodIndex, consumed by the ABA tip-to-base force pass.
vendor multibody/tree/articulated_body_inertia.cc  # Contains only the explicit template instantiation of ArticulatedBodyInertia on the default scalars, which ORVD narrows to double alone.
vendor multibody/tree/articulated_body_inertia.h  # Implements the ArticulatedBodyInertia value type: a lower-triangular-stored symmetric 6x6 that supports construction from SpatialInertia, Shift/ShiftInPlace across a mobilizer, and operator+= used by the ABA inertia recursion.
vendor multibody/tree/articulated_body_inertia_cache.cc  # Contains only the explicit template instantiation of ArticulatedBodyInertiaCache on the default scalars, which ORVD narrows to double alone.
vendor multibody/tree/articulated_body_inertia_cache.h  # Holds the first-ABA-pass results per mobod: P_B_W, Pplus_PB_W, the Eigen::LLT factorization of the hinge inertia D_B (via math::LinearSolver), and the Kalman gain g_PB_W = P_B_W*H_PB_W*D_B^-1.
vendor multibody/tree/ball_rpy_joint.cc  # Out-of-line half of BallRpyJoint: type_name(), the TemplatedDoCloneToScalar/DoCloneToScalar/DoShallowClone set that carries damping, limits and default positions across scalars, and MakeMobilizerForJoint, which picks inboard/outboard frames via tree_frames(mobod.is_reversed()) and constructs the RpyBallMobilizer the finalized tree actually runs.
vendor multibody/tree/ball_rpy_joint.h  # User-facing 3-DOF Joint that wraps RpyBallMobilizer with space x-y-z Euler angle coordinates, per-axis damping, and AddInTorque into MultibodyForces; it takes systems::Context<T> in its accessor signatures only through the Joint base typedef, which ORVD replaces with its own state contract.
vendor multibody/tree/block_system_jacobian_cache.cc  # Implements the constructor that sizes and zeroes one Jacobian block per SpanningForest tree and ToFullMatrix(), which assembles the block-diagonal blocks into a dense 6*num_mobods x nv matrix with World's leading six rows left zero.
vendor multibody/tree/block_system_jacobian_cache.h  # Declares the per-tree block storage of the system Jacobian Jv_V_WB (6*n_i x m_i MatrixX per SpanningForest tree) plus ToFullMatrix(), which is exactly the Jacobian capability ORVD needs.
vendor multibody/tree/body_node.cc  # Implements CalcCompositeBodyInertiaInWorld_TipToBase (the composite-body-inertia recursion that shifts each child's K_CCo_W to Bo for the mass matrix pass) and CalcArticulatedBodyHingeInertiaMatrixFactorization (LLT of D_B with the positive-definiteness diagnostic).
vendor multibody/tree/body_node.h  # Declares the BodyNode recursion interface (body/parent/mobilizer wiring, child_nodes, GetJacobianFromArray) and the virtual base-to-tip / tip-to-base entry points for position, velocity, acceleration, mass matrix, inverse dynamics, ABA inertia/force/acceleration, and composite body inertia.
vendor multibody/tree/body_node_impl.cc  # Defines the fixed-size per-mobilizer bodies of CalcPositionKinematicsCache_BaseToTip, CalcAcrossNodeJacobianWrtVExpressedInWorld, CalcVelocityKinematicsCache_BaseToTip, CalcSpatialAcceleration_BaseToTip, CalcInverseDynamics_TipToBase, the three ABA passes, and CalcSpatialAccelerationBias, then explicitly instantiates them for all 14 mobilizer types (drop the AutoDiffXd and symbolic::Expression instantiation lines at the bottom).
vendor multibody/tree/body_node_impl.h  # Declares BodyNodeImpl<T, ConcreteMobilizer>, the mobilizer-templated final BodyNode that inherits kNq/kNv/kNx and QVector/VVector/HMatrix fixed-size types so every recursion runs on compile-time-sized blocks.
vendor multibody/tree/body_node_impl_mass_matrix.cc  # Split-out continuation of body_node_impl.cc that defines CalcMassMatrixContributionViaWorld_TipToBase and the six CalcMassMatrixOffDiagonalBlockViaWorld<Bnv> variants forming the O(n^2)-via-World mass matrix assembly, with the same 14-mobilizer explicit instantiations (drop the AutoDiffXd and symbolic lines).
vendor multibody/tree/body_node_world.h  # Provides BodyNodeWorld, the zero-dof terminal node for the World mobod whose overrides are all DRAKE_UNREACHABLE so the recursions can stop at Mobod 0 without a null check.
vendor multibody/tree/curvilinear_joint.cc  # Out-of-line CurvilinearJoint bodies: the constructor that stores the PiecewiseConstantCurvatureTrajectory and the arc-length position limits (rejecting negative damping), type_name(), the clone-to-scalar paths, and MakeMobilizerForJoint which hands curvilinear_path_ to a new CurvilinearMobilizer during finalization.
vendor multibody/tree/curvilinear_joint.h  # Single-DOF Joint whose coordinate is arc length s along a PiecewiseConstantCurvatureTrajectory, i.e. exactly the along-track motion primitive a rail vehicle model wants; note it drags in drake/common/trajectories/piecewise_constant_curvature_trajectory.h, which itself includes drake/systems/framework/scalar_conversion_traits.h, so that trajectory file must be vendored or reimplemented too.
vendor multibody/tree/curvilinear_mobilizer.cc  # Defines X_FM(q), H_FM/Hdot_FM, ProjectSpatialForce, and the N/N+ matrices for arc-length parameterization by delegating to the curvature trajectory; its AutoDiffXd/symbolic DoCloneToScalar bodies and the DEFINE_..._ON_DEFAULT_SCALARS tail are the parts deleted for a double-only build.
vendor multibody/tree/curvilinear_mobilizer.h  # Declares the single-dof mobilizer whose hinge matrix in M is H_FM_M = [0 0 ρ(q) 1 0 0]ᵀ, moving frame M by arc length q along a planar piecewise-constant-curvature path — the natural track-following mobilizer for a rail vehicle; note it costs a dependency on drake/common/trajectories/piecewise_constant_curvature_trajectory.h (which itself pulls drake/systems/framework/scalar_conversion_traits.h), and it is named in the BodyNodeImpl instantiation macro so dropping it would also require editing body_node_impl.cc and body_node_impl_mass_matrix.cc.
forbidden multibody/tree/deformable_body.cc  # Instantiates concrete FEM constitutive models (corotated, neohookean, linear), quadrature rules and the Newmark scheme, and registers deformable geometry with SceneGraph.
forbidden multibody/tree/deformable_body.h  # Owns an fem::FemModel plus a geometry::GeometryId over a volume mesh and MultibodyPlant deformable-constraint specs — three separately forbidden edges (fem, geometry, plant) in one header.
discard multibody/tree/door_hinge.cc  # Implements CalcApproximationCurves — the tanh(x/t) sigmoid with its singlet/doublet derivative curves — and the CalcHingeSpringTorque / CalcHingeFrictionalTorque / CalcHingeTorque built from it plus the potential-energy and conservative/non-conservative power terms; only useful if the door-hinge element itself is ever wanted.
discard multibody/tree/door_hinge.h  # A ForceElement that stacks empirically-tuned door torques (torsional spring toward a zero angle, static/dynamic/viscous friction smoothed by a motion threshold, and a catch torque near closed) onto one RevoluteJoint; a rail vehicle has no door-hinge model, and it has no forbidden includes so it can be re-adjudicated later.
vendor multibody/tree/element_collection.cc  # Out-of-line template definitions (Add/Rename/RemoveIfInvalid/index rebuild) plus the explicit instantiations for Frame, Joint, JointActuator, ModelInstance and Link; the trailing ElementCollection<T, DeformableBody, DeformableBodyIndex> instantiation block and its deformable_body.h include are the only forbidden contact and must be deleted when vendoring — the header itself is clean.
vendor multibody/tree/element_collection.h  # The container MultibodyTree stores its frames, joints, actuators, model instances and links in: index-addressed slots that tolerate holes, a packed iteration view, O(1) lookup by element->name() via a string hash map, and mixed owned/unowned ownership (needed because a RigidBodyFrame is owned by its RigidBody, not by the collection).
vendor multibody/tree/fixed_offset_frame.cc  # Implements get/set of X_PF through a systems::BasicVector numeric parameter and DoCalcPoseInBodyFrame/DoCalcRotationMatrixInBodyFrame which recurse up the parent-frame chain to produce X_BF; the numeric-parameter storage is exactly what ORVD must remap onto its own parameter contract.
vendor multibody/tree/fixed_offset_frame.h  # The Frame subclass carrying a constant pose X_PF relative to a parent frame, stored as a 12-element numeric parameter so it can be retuned post-construction; this is the frame type used for essentially every joint frame F/M offset on a car body, bolster or axlebox.
forbidden multibody/tree/force_density_field.cc  # Defines the ForceDensityField/GravityForceField members declared in the forbidden fem-derived header, forwarding cache-entry and input-port declarations through MultibodyTreeSystemElementAttorney.
forbidden multibody/tree/force_density_field.h  # Extends the FEM base class fem::ForceDensityFieldBase to supply body-force density [N/m³] over a deformable volume, and its DeclareCacheEntries/DeclareInputPorts API is typed on MultibodyPlant<T>*; GravityForceField here is the deformable per-volume gravity, not the rigid-body UniformGravityFieldElement ORVD needs.
vendor multibody/tree/force_element.cc  # Provides the ForceElement destructor and the ShallowClone NVI whose default DoShallowClone throws a NiceTypeName-formatted 'failed to override' error; small but required to link the base class.
vendor multibody/tree/force_element.h  # The abstract base every force element ORVD needs derives from — it fixes the CalcAndAddForceContribution(context, position cache, velocity cache, MultibodyForces*) contract plus the potential-energy / conservative-power / non-conservative-power trio used for energy bookkeeping; gravity, linear spring-dampers and the roll-pitch-yaw bushing are all subclasses.
vendor multibody/tree/frame.cc  # Implements the relative-motion algebra — CalcAngularVelocity via w_MF = w_WF - w_WM re-expressed in E, CalcSpatialVelocityInWorld/CalcSpatialAccelerationInWorld by shifting the body's V_WB/A_WB along p_BF_W, and the rearranged-composition CalcSpatialVelocity/CalcSpatialAcceleration including the 2*w_WM x v_MFo Coriolis term — plus scoped_name() and internal::DeprecateWhenEmptyName(), the empty-name guard that both frame.h and rigid_body.cc call from their constructors; it instantiates both a hand-picked function list and the whole Frame class.
vendor multibody/tree/frame.h  # Defines the material-frame abstraction: name plus the owning Link reference, the parameterized X_BF/X_FB/is_X_BF_identity accessors read out of FrameBodyPoseCache, and the user-facing CalcPose / CalcSpatialVelocity / CalcSpatialAcceleration queries relative to arbitrary measured-in and expressed-in frames; it types its Calc* API on systems::Context and its parameter NVIs on systems::Parameters without including any drake/systems header directly, so the systems coupling is by type name only.
vendor multibody/tree/frame_body_pose_cache.cc  # Nothing but the explicit template instantiation of FrameBodyPoseCache on the default scalars; in a double-only build this file collapses to a single instantiation or disappears entirely.
vendor multibody/tree/frame_body_pose_cache.h  # The parameter-dependent precomputation pool holding X_LF (frame pose on its link), X_BL/X_BF/X_FB (frame pose on its mobilized body, with identity flags for the fast path), and the mass properties M_LLo_L, M_BBo_B and p_BoLcm_B — the composite-mobod-aware inertia and pose data that position kinematics reads instead of recomputing each step.
forbidden multibody/tree/geometry_spatial_inertia.cc  # Integrates inertia over mesh tetrahedra by loading OBJ/VTK meshes and falling back to a convex hull, pulling four drake/geometry/proximity headers.
forbidden multibody/tree/geometry_spatial_inertia.h  # Its entire purpose is turning a geometry::Shape or geometry::TriangleSurfaceMesh into a SpatialInertia at a given density, so every declaration takes a drake/geometry type.
vendor multibody/tree/joint.cc  # Implements the q/v accessors that slice the tree-wide state vector through the joint's mobilizer (get_positions_from_array / get_mutable_velocities_from_array) plus SetPose/GetPosePair/SetSpatialVelocity forwarding, with ThrowIfNotFinalized guards; the state slicing is the piece ORVD must re-point at its own state container instead of systems::Context.
vendor multibody/tree/joint.h  # The user-facing kinematic-pair abstraction over a mobilizer: owns frames F on parent and M on child, per-dof position/velocity/acceleration limits, default damping and default positions, the lock/unlock flag, and AddInOneForce/AddInDamping which is how spring and damper torques enter generalized forces — the base class for every revolute, prismatic and floating joint in a bogie model.
vendor multibody/tree/joint_actuator.cc  # Out-of-line JointActuator bodies: joint() resolved through get_parent_tree().get_joint(joint_index_), AddInOneForce forwarding tau to Joint::AddInOneForce, set_actuation_vector writing u->segment(input_start(), num_inputs()), num_inputs() = joint().num_velocities(), the effort-limit and PD-controller-gain validation, and the DoCloneToScalar bodies.
vendor multibody/tree/joint_actuator.h  # Declares JointActuator, which owns the effort limit, per-actuator dof offset into u, AddInOneForce() that injects tau into MultibodyForces, and the rotor-inertia/gear-ratio pair whose product rho^2*Ir is the reflected inertia added to the mass-matrix diagonal.
vendor multibody/tree/linear_bushing_roll_pitch_yaw.cc  # Implements the bushing math: CalcR_AB for the halfway frame B midway between Ao and Co, the rpy angles and rates, tau = -(K012*q + D012*qdot) converted to a moment by t_Cp_A = Nᵀ*tau (N from RollPitchYaw::CalcMatrixRelatingRpyDtToAngularVelocityInParent), the gimbal-lock tolerance throw, and DoCalcAndAddForceContribution which shifts the resulting spatial force from Ao to link0's origin and adds the negated further-shifted copy at link1's origin.
vendor multibody/tree/linear_bushing_roll_pitch_yaw.h  # Declares the six-axis roll-pitch-yaw bushing force element with diagonal torque/force stiffness and damping matrices stored as runtime numeric parameters, i.e. exactly the compliant connection element ORVD needs for primary/secondary suspension bushings.
vendor multibody/tree/linear_spring_damper.cc  # Implements DoCalcAndAddForceContribution computing f_AP = (k*(l-l0) + c*dl/dt)*r_hat and shifting it into the per-body spatial-force array, plus CalcPotentialEnergy and CalcConservativePower for energy bookkeeping.
vendor multibody/tree/linear_spring_damper.h  # Declares the two-point finite-free-length spring-damper (points P on body A and Q on body B, parameters l0, k, c), the axial force element used for ORVD traction rods and vertical/lateral dampers.
vendor multibody/tree/mobilizer.cc  # Provides Mobilizer's out-of-line pieces: GetPosePair() converting X_FM into (quaternion, translation) and the throwing default bodies for DoMapAccelerationToQDDot, DoMapQDDotToAcceleration, DoCalcNDotMatrix and DoCalcNplusDotMatrix.
vendor multibody/tree/mobilizer.h  # Defines the abstract across-mobilizer kinematics contract every joint type implements: X_FM(q), hinge matrix H_FM and its time derivative, ProjectSpatialForce (tau = H_FM^T * F), and the N(q)/N+(q) qdot-v coupling matrices; it uses systems::Context/State for q,v access but is otherwise pure kinematics.
vendor multibody/tree/mobilizer_impl.cc  # Implements SetZeroState/set_default_state/set_random_state and the pose-to-q and spatial-velocity-to-v inverse hooks, and carries the explicit instantiation list for the (nq,nv) sizes {0,0},{1,1},{2,2},{3,3},{6,6},{7,6} that every concrete mobilizer links against.
vendor multibody/tree/mobilizer_impl.h  # The MobilizerImpl<T, nq, nv> layer that fixes nq/nv at compile time, gives each concrete mobilizer typed fixed-size QVector/VVector views into the state, and documents the calc_X_FM / calc_V_FM / calc_A_FM / calc_tau inline contract that body_node_impl dispatches to allocation-free.
vendor multibody/tree/model_instance.cc  # Implements the per-instance gather/scatter of q, v and u by walking this instance's mobilizers and actuators and copying their contiguous blocks in and out of the model-wide arrays.
vendor multibody/tree/model_instance.h  # Declares ModelInstance, which accumulates the per-submodel counts of q, v and actuated dofs and provides Get/Set PositionsFromArray, VelocitiesFromArray and ActuationInArray slicing — the mechanism ORVD needs to address one bogie or one carbody's coordinates inside the whole-vehicle state vector.
vendor multibody/tree/multibody_element.cc  # Implements the element lifetime and ownership checks (HasThisParentTreeOrThrow, ThrowNoParentTree, nulling parent_tree_ on destruction) plus the DeclareNumericParameter/DeclareAbstractParameter/DeclareDiscreteState/DeclareCacheEntry forwarders that go through MultibodyTreeSystemElementAttorney — only that attorney half needs rewriting.
vendor multibody/tree/multibody_element.h  # Base class giving every tree element its ModelInstanceIndex, its type-safe index_impl<>, the is_ephemeral flag distinguishing Finalize-added joints/frames/mobilizers from user-added ones, and the parent-tree back pointer; its DeclareParameters/DeclareDiscreteState/DeclareCacheEntries hooks and the plant-only GetParentPlant() declaration must be re-pointed at ORVD's own runtime contract.
vendor multibody/tree/multibody_forces.cc  # Implements the constructors that size F_B_W to num_mobods and tau to num_velocities from a tree or tree-system, plus AddInForces and the CheckHasRightSizeForModel size guards.
vendor multibody/tree/multibody_forces.h  # Declares the force accumulator that every force element writes into: a per-MobodIndex std::vector<SpatialForce> F_B_W plus the generalized-force vector tau, with SetZero and AddInForces superposition.
vendor multibody/tree/multibody_tree-inl.h  # Holds the template bodies MultibodyTree's header only declares: AddFrame/AddMobilizer/AddJoint/AddEphemeralJoint/AddForceElement/AddJointActuator/AddModelInstance construction and registration, and the get_positions/get_velocities/get_mutable_* accessors that carve q and v out of the state vector.
vendor multibody/tree/multibody_tree.cc  # Contains the actual recursions: base-to-tip and tip-to-base sweeps over BodyNodes for kinematics, the composite-body and ABA passes, force-element accumulation, spatial momentum and center-of-mass computations, and the ephemeral-joint/mobilizer synthesis performed during Finalize.
vendor multibody/tree/multibody_tree.h  # Declares MultibodyTree itself: element ownership and name lookup, Finalize/topology, the position/velocity/acceleration kinematics cache passes, CalcMassMatrix and CalcMassMatrixViaInverseDynamics, CalcInverseDynamics, CalcBiasTerm, the full articulated-body-algorithm triple (inertia cache, force bias, force cache, accelerations), and all CalcJacobian* variants — the single most load-bearing header in this extraction.
vendor multibody/tree/multibody_tree_indexes.h  # Defines the TypeSafeIndex aliases the entire tree is addressed by (BodyIndex/LinkIndex, LinkOrdinal, FrameIndex, JointIndex/JointOrdinal, ForceElementIndex, JointActuatorIndex, ModelInstanceIndex, internal MobodIndex and TreeIndex) plus world_index()/world_mobod_index(); the DeformableBodyId and DeformableBodyIndex aliases here are bare type declarations with no deformable dependency and should simply be deleted.
first_party multibody/tree/multibody_tree_system.cc  # Besides the ~18 DeclareCacheEntry/DeclareContinuousState declarations in Finalize(), this file holds DeclareMultibodyElementParameters() and SetDefaultParameters() — the only loops that declare and default every element's numeric parameters (RigidBody mass/com/inertia, FixedOffsetFrame X_PF, joint damping, spring k/theta0, bushing K/D, actuator rotor inertia) — plus the ABA forward-dynamics driver CalcArticulatedBodyForceCache/CalcForwardDynamicsContinuous that chains CalcForceElementsContribution into the tree's ABA force pass and acceleration pass; ORVD must reimplement all three responsibilities, not just the cache/state manager.
first_party multibody/tree/multibody_tree_system.h  # Entire class is the LeafSystem wrapper that owns a MultibodyTree and declares its Context resources: it derives from systems::LeafSystem<T> and exposes EvalFrameBodyPoses/EvalPositionKinematics/EvalVelocityKinematics/EvalForwardDynamics as CacheEntry::Eval calls, so ORVD must reimplement this layer against its own runtime/cache contract.
first_party multibody/tree/parameter_conversion.h  # Its whole content is packing/unpacking a SpatialInertia into a 10-coordinate systems::BasicVector so mass properties can live as a Context numeric parameter; ORVD needs the same 10-element layout but over its own parameter storage, not BasicVector.
vendor multibody/tree/planar_joint.cc  # Provides PlanarJoint::MakeMobilizerForJoint, which is how the tree builder turns a planar joint into the PlanarMobilizer that actually supplies X_FM/V_FM during finalization.
vendor multibody/tree/planar_joint.h  # Defines the 3-DOF planar Joint (x, y translation in F's x-y plane plus θ about Fz) with per-axis viscous damping and the DoAddInOneForce/DoAddInDamping hooks that inject joint forces into MultibodyForces.
vendor multibody/tree/planar_mobilizer.cc  # Implements the planar mobilizer's N, N⁺, Ṅ, Ṅ⁺ matrices and MapVelocityToQDot/MapQDotToVelocity/MapAccelerationToQDDot plus CreateBodyNode, which binds the mobilizer into the BodyNode recursion.
vendor multibody/tree/planar_mobilizer.h  # MobilizerImpl<T,3,3> supplying the inlined calc_X_FM/calc_V_FM/calc_A_FM/calc_tau kernels for the planar hinge matrix that the O(n) position/velocity/acceleration and inverse-dynamics recursions call per node.
vendor multibody/tree/position_kinematics_cache.cc  # Two-line translation unit that emits the explicit template instantiations of PositionKinematicsCache on the default scalars; ORVD keeps it (trimmed to double) so the pools are compiled once rather than per include.
vendor multibody/tree/position_kinematics_cache.h  # The per-mobod pose pools (X_WB, X_WL by LinkOrdinal, X_PB, X_FM, p_PoBo_W) written by the base-to-tip position pass and read by every Jacobian, mass-matrix and inverse-dynamics pass.
vendor multibody/tree/prismatic_joint.cc  # Normalizes the user-supplied axis and implements MakeMobilizerForJoint, which selects the axis-specialized PrismaticMobilizerAxial<T,0/1/2> (or the general one) for the finalized tree.
vendor multibody/tree/prismatic_joint.h  # Defines the 1-DOF sliding Joint along axis â with position/velocity/acceleration limits and a Context-stored damping parameter (GetDamping/SetDamping) — the primary joint type for rail-vehicle vertical and lateral suspension travel.
vendor multibody/tree/prismatic_mobilizer.cc  # Implements the translation accessors, the identity N/N⁺ (and zero Ṅ/Ṅ⁺) maps, and the explicit instantiations of PrismaticMobilizerAxial for axes 0, 1 and 2 that the joint factory hands out.
vendor multibody/tree/prismatic_mobilizer.h  # MobilizerImpl<T,1,1> base plus the PrismaticMobilizerAxial<T,axis> template whose compile-time axis lets calc_X_FM/calc_V_FM/calc_A_FM/calc_tau reduce to a single-component update in the hot recursion.
vendor multibody/tree/prismatic_spring.cc  # Implements DoCalcAndAddForceContribution writing -k(x-x₀) into the generalized-force vector, plus CalcPotentialEnergy = ½k(x-x₀)² and the conservative/non-conservative power terms used for energy checks.
vendor multibody/tree/prismatic_spring.h  # ForceElement that applies f = -k(x - x₀) to a PrismaticJoint's generalized coordinate — the force element ORVD needs for suspension coil/air-spring stiffness along a slider.
vendor multibody/tree/quaternion_floating_joint.cc  # Implements MakeMobilizerForJoint for the floating base and DoAddInOneForce/DoAddInDamping, which apply the six-component damping wrench directly as generalized forces on the floating mobilizer.
vendor multibody/tree/quaternion_floating_joint.h  # The 6-DOF free Joint with 7 positions (q_FM wxyz then p_FM) and 6 velocities (w_FM, v_FM) — how a carbody or wheelset floating base is attached to world with separate angular and translational damping.
vendor multibody/tree/quaternion_floating_mobilizer.cc  # Contains the CalcQMatrix/CalcScaledQMatrix quaternion-rate kernels and the N, N⁺, Ṅ, Ṅ⁺ implementations (including the normalizing variant) required for correct q̇ integration and qddot mapping of a floating base.
vendor multibody/tree/quaternion_floating_mobilizer.h  # MobilizerImpl<T,7,6> whose N/N⁺ maps convert between (w_FM, v_FM) and quaternion time derivatives q̇ — the nq≠nv case that the whole tree's MapVelocityToQDot machinery exists to handle.
vendor multibody/tree/revolute_joint.cc  # Normalizes the revolute axis and implements MakeMobilizerForJoint, dispatching to the axis-specialized RevoluteMobilizerAxial when â is a coordinate axis.
vendor multibody/tree/revolute_joint.h  # Defines the 1-DOF hinge Joint about axis â with angle limits and Context-stored viscous damping τ = -d·ω — the joint type for bogie pitch/yaw and axle rotation.
vendor multibody/tree/revolute_mobilizer.cc  # Out-of-line revolute across-mobilizer kinematics: get_angle/SetAngle/get_angular_rate/SetAngularRate state access, RevoluteMobilizerAxial's CalcAcrossMobilizerTransform/SpatialVelocity/SpatialAcceleration and ProjectSpatialForce delegating to the inline calc_* kernels, the identity N/N+ with zero Ndot/Nplusdot and the qdot<->v maps, and the CreateBodyNode() factory returning BodyNodeImpl<T,RevoluteMobilizerAxial> with the axis 0/1/2 explicit instantiations.
vendor multibody/tree/revolute_mobilizer.h  # Declares RevoluteMobilizer plus the axis-specialized RevoluteMobilizerAxial<T,axis> that supplies X_FM as a fast axial-rotation transform and the constant hinge matrix H_FM=[axis;0] consumed by every position/velocity/acceleration kinematics pass.
vendor multibody/tree/revolute_spring.cc  # Implements DoCalcAndAddForceContribution adding the spring torque into MultibodyForces generalized forces, the potential-energy/conservative-power accounting, and the parameter declaration and clone paths.
vendor multibody/tree/revolute_spring.h  # ForceElement subclass computing torsional spring torque tau = -k*(theta - theta0) on a RevoluteJoint, with k and theta0 stored as context numeric parameters — directly the spring force-element capability ORVD requires.
vendor multibody/tree/rigid_body.cc  # Out-of-line RigidBody constructors, scoped_name() resolved through the model_instance lookup, and the DoCloneToScalar/DoShallowClone implementations for both RigidBody and RigidBodyFrame.
vendor multibody/tree/rigid_body.h  # Declares RigidBody (default SpatialInertia, per-context mass/com/rotational-inertia parameters, mobilizer Lock/Unlock, floating-base q/v offsets) and RigidBodyFrame, the body-fixed frame every other frame is measured against.
vendor multibody/tree/rotational_inertia.cc  # Implements CalcPrincipalMomentsAndMaybeAxesOfInertia using Eigen's SelfAdjointEigenSolver (Eigen only, not drake/solvers), ReExpressInPlace, and CreateInvalidityReport for the physical-validity diagnostics.
vendor multibody/tree/rotational_inertia.h  # The symmetric 3x3 inertia matrix value class with ReExpress, ShiftFromCenterOfMass/ShiftToCenterOfMass and the triangle-inequality physical-validity test that SpatialInertia and UnitInertia are built on.
vendor multibody/tree/rpy_ball_mobilizer.cc  # Implements CalcAcrossMobilizerTransform/SpatialVelocity/SpatialAcceleration, ProjectSpatialForce, the N and N+ matrices with their time derivatives, and the BodyNodeImpl<T,RpyBallMobilizer> factory that body_node_impl.cc explicitly instantiates.
vendor multibody/tree/rpy_ball_mobilizer.h  # 3-dof gimbal mobilizer parameterizing R_FM by space-fixed x-y-z angles with v = w_FM, giving H_FM_F = [I3;0] and the N(q)/N+(q) maps that are singular at pitch = pi/2 + k*pi.
vendor multibody/tree/rpy_floating_joint.cc  # Out-of-line RpyFloatingJoint bodies: type_name(), the TemplatedDoCloneToScalar/DoCloneToScalar/DoShallowClone set that carries damping and default positions across scalars, and MakeMobilizerForJoint which builds the RpyFloatingMobilizer on the frames chosen by tree_frames(mobod.is_reversed()).
vendor multibody/tree/rpy_floating_joint.h  # Public 6-dof Joint whose q is (roll,pitch,yaw,p_FM) and v is (w_FM,v_FM); multibody_tree.cc creates this joint type as the ephemeral base-body joint when ForestBuildingOptions::kUseRpyFloatingJoints is selected.
vendor multibody/tree/rpy_floating_mobilizer.cc  # Implements the across-mobilizer transform/velocity/acceleration, ProjectSpatialForce, CalcNMatrix/CalcNplusMatrix and their derivatives, and the BodyNodeImpl<T,RpyFloatingMobilizer> factory that multibody_tree.cc dynamic_casts to in the free-body pose setters.
vendor multibody/tree/rpy_floating_mobilizer.h  # 6-dof mobilizer with rpy orientation plus p_FM translation, supplying X_FM, H_FM and the kinematic map N(q) between w_FM and roll-pitch-yaw rates — the free-flight mobilizer a carbody or bogie frame rides on.
vendor multibody/tree/scoped_name.cc  # Implements Make/Join/Parse string splitting on the '::' delimiter and the validation that throws when a namespace starts/ends with '::' or an element name is empty.
vendor multibody/tree/scoped_name.h  # Value class splitting 'model_instance::element' into namespace and element halves; RigidBody::scoped_name() and Frame::scoped_name() return it, so the header is in the compile closure of rigid_body.h and frame.h.
vendor multibody/tree/screw_joint.cc  # Out-of-line ScrewJoint bodies: the constructor that throws on a zero-length axis or negative damping and stores axis.normalized() plus screw_pitch_, type_name(), clone-to-scalar, and MakeMobilizerForJoint which passes screw_axis() and screw_pitch_ into a new ScrewMobilizer.
vendor multibody/tree/screw_joint.h  # Public 1-dof Joint coupling rotation about an axis to translation along it via a screw pitch, exposing get_translation/set_translation derived from theta; uncertain for rail vehicles specifically since no rail model uses a screw joint, but it is the only public constructor for the ScrewMobilizer that the body-node instantiation list already requires.
vendor multibody/tree/screw_mobilizer.cc  # Implements the across-mobilizer transform/velocity/acceleration and the BodyNodeImpl<T,ScrewMobilizer> factory; body_node_impl.cc unconditionally instantiates BodyNodeImpl<T,ScrewMobilizer<T>>, so dropping this file breaks the node instantiation list.
vendor multibody/tree/screw_mobilizer.h  # 1-dof screw mobilizer whose hinge matrix is H_FM = [axis; (pitch/2pi)*axis], plus the free functions GetScrewTranslationFromRotation/GetScrewRotationFromTranslation shared with ScrewJoint.
vendor multibody/tree/spatial_inertia.cc  # Implements the closed-form uniform-density factories (box, cube, cylinder, capsule, ellipsoid, sphere, tetrahedron, thin rod), IsPhysicallyValid/ThrowIfNotPhysicallyValid, and CalcPrincipalHalfLengthsAndPoseForEquivalentShape; the geometry::Shape and TriangleSurfaceMesh overloads live in geometry_spatial_inertia.h, so nothing here pulls drake/geometry.
vendor multibody/tree/spatial_inertia.h  # The 6x6 spatial inertia M_SP_E stored as mass, p_PScm and UnitInertia, with ReExpress/Shift and operator*(SpatialAcceleration) — the per-body quantity that inverse dynamics, the composite-body mass matrix and articulated body inertia all consume.
vendor multibody/tree/uniform_gravity_field_element.cc  # Implements AccumulateGravitySpatialForces (m*g at each body's center of mass shifted to Bo, accumulated into the F_Bo_W array) and the tau_g(q) = J_v^T * f projection used for gravity compensation and potential energy.
vendor multibody/tree/uniform_gravity_field_element.h  # Declares the ForceElement subclass that holds the constant g_W vector plus a per-model-instance disable set, and exposes CalcGravityGeneralizedForces / CalcPotentialEnergy / CalcConservativePower hooks that the tree calls to add gravity to the force accumulation.
vendor multibody/tree/unit_inertia.cc  # Out-of-line bodies of the shape constructors (SolidEllipsoid, SolidBox, SolidCylinder, SolidCapsule, tetrahedron and AxiallySymmetric variants) including their unit-vector normalization and validity checks.
vendor multibody/tree/unit_inertia.h  # Provides the per-unit-mass rotational inertia G_BP with ReExpress / ShiftFromCenterOfMass / ShiftToCenterOfMass and the closed-form shape formulas (SolidCylinder, ThinRod, AxiallySymmetric, SolidBox, PointMass) used to construct SpatialInertia for rigid bodies; the word 'geometry' appears only in a doc comment, there is no drake/geometry include.
vendor multibody/tree/universal_joint.cc  # Defines type_name "universal", the scalar-conversion clones, and MakeMobilizerForJoint which instantiates the UniversalMobilizer on the inboard/outboard frames chosen by the forest.
vendor multibody/tree/universal_joint.h  # User-facing 2-DOF Joint wrapper exposing get/set angles and angular rates, damping, and joint limits, and forwarding to a UniversalMobilizer for the R_FM(theta0,theta1) = Rx(theta0)*Ry(theta1) parameterization; it aliases systems::Context but pulls it transitively through joint.h rather than including it.
vendor multibody/tree/universal_mobilizer.cc  # Implements CalcHwMatrix and its time derivative, CalcAcrossMobilizerTransform/SpatialVelocity/SpatialAcceleration, ProjectSpatialForce (tau = Hw^T * t), and the identity N/N+ matrices since v = qdot for this mobilizer.
vendor multibody/tree/universal_mobilizer.h  # MobilizerImpl<T,2,2> specialization whose inline calc_X_FM/update_X_FM and the documented Hw_FM_F = [[1,0],[0,cos q0],[0,sin q0]] hinge matrix supply the across-mobilizer kinematics for a universal joint; the systems/framework/context.h include is only for the Context& arguments on the accessor API and is replaceable by the ORVD runtime contract.
vendor multibody/tree/velocity_kinematics_cache.cc  # Two-line translation unit that emits the explicit template instantiations of VelocityKinematicsCache on the default scalars; trivially reducible to double-only.
vendor multibody/tree/velocity_kinematics_cache.h  # Pool storage for the velocity-kinematics results V_WB, V_WL, V_PB_W and V_FM, sized from the SpanningForest and indexed by MobodIndex/LinkOrdinal; the base-to-tip velocity pass writes into it and mass-matrix, Coriolis and inverse-dynamics passes read from it.
vendor multibody/tree/weld_joint.cc  # Defines type_name "weld", the scalar clones, and MakeMobilizerForJoint which folds the stored X_FM into the frames handed to a WeldMobilizer.
vendor multibody/tree/weld_joint.h  # Zero-DOF Joint that stores a fixed X_JpJc transform between the parent and child frames; this is how the rail-vehicle assembly template rigidly attaches subassembly frames without adding state.
vendor multibody/tree/weld_mobilizer.cc  # Implements the zero across-mobilizer spatial velocity and acceleration, the no-op ProjectSpatialForce and N/N+ matrices, and CreateBodyNode for the 0-DOF body node specialization.
vendor multibody/tree/weld_mobilizer.h  # MobilizerImpl<T,0,0> whose calc_X_FM returns identity and whose post_multiply_by_X_FM/pre_multiply_by_X_FM shortcuts skip the transform composition entirely, which is what makes welded composite bodies cheap in the position pass; the systems/framework/context.h include is only for Context& signatures.
